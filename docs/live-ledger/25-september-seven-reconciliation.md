# 2026-09-07：港股简称漏筛与盘中对账恢复

## 实际状态

- 服务器及直连QMT处于ready，但StrategyLedger被盘中对账阻断，不能称为整条交易链路正常。
- 09:30:15对账READY，09:30:18起出现粮食ETF博时159031和粮食ETF国泰159033的缺价成交及持仓差异。
- 柜台显示两笔卖出全部成交；只读复查原生xtquant回报，成交量有效，成交价和成交金额仍无正数。柜台委托价和本地订单限价也没有可用估值，不能沿用此前的委托价保守估算。
- 港红利520890卖出有有效成交价并已入账；本轮QMT只有卖单，没有发出新标的买单。JQ三卖三买正常完成。
- 本次没有下单、撤单、手工调整账本或更改服务器交易开关。

## 修改计划与实现

1. **过滤纠错**：helper公共规则增加HK、沪深港，代码兜底补充513320及159322。保留自定义关键词/排除表覆盖能力，不用513前缀过滤所有海外基金。
2. **执行前重新对账**：删除目标提交入口对进程级旧`startup_ready`布尔值的直接拒绝，改为本账户每次提交/自动推进前先同步最新柜台证据。最新结果仍BLOCKED时，在估值、规划、自动撤旧单、发单之前停止。
3. **准确报错**：错误给出strategy_id、当前对账状态和blockers；缺价成交同时给出证券代码，说明“成交待核实，不能按0元入账”。不再把盘中阻断称为启动失败。
4. **边界与复审**：不改策略正文，不改定价、订单类型、追单、排序或权重公式。读取快照和显式撤单保留原行为，不放开对账门禁，不新增后台轮询或新模式。

### 过滤依据

- [上交所513320上市说明](https://etf.sse.com.cn/fundtrends/c/5699435.shtml)：场内简称HK新经济，对应恒生港股通新经济ETF。
- [平安基金159322产品说明](https://fund.pingan.com/main/peanutFinance/yingPeanut/fundDetailV2/159322.shtml)：标的为中证沪深港黄金产业股票指数，投资范围包括港股。沿用原先也排除沪港深类的策略口径，不是把所有黄金ETF排除。
- 指数元数据无法覆盖所有简称；这次补齐已确认缺口，不宣称关键词识别能完整识别所有未来上市ETF。

## 验证要求

本地356项回归全部通过，聚宽单文件导出、关键语法/未定义名称检查及`git diff --check`通过。复审确认执行前使用最新对账结果，仍保留planner/outbox原有权限、归属与幂等检查。测试中的有效后续价格为构造的柜台证据，不是声称服务器已收到真实价格。

- HK简称、沪深港指数命中；普通黄金ETF、纳指ETF、港口航运ETF不误筛。
- 三个已确认简称漏筛场景，各自验证剩余池、流动性过滤、精确排序和权重不变。
- 旧startup标志为false但当前对账READY：可以继续。
- 最新对账仍失败：输出具体原因，不进入估值/发单/自动撤旧单。
- 零价成交重复重放不会按0元改现金或持仓；收到同一成交的有效价格后才入账，重复重放不重复记账或发单。

## 尚未解决的外部问题

代码不能生成柜台缺失的真实成交价。本次没有把两笔待核实卖出伪装成已入账，也未把未知费用记为已知0。
需要QMT客户端/柜台提供这两笔交易的有效成交或交割记录。如果柜台也无法给出有效数据，应先检查模拟柜台市价撮合/回报能力；改变市价为限价属于独立的下单行为变更，本次不做。

新的程序在后续提交目标或收到委托/成交事件时可重新检查；只有有效证据到达且对账恢复READY，执行才可继续。这不是保证零价回报会自动恢复，也不是追加每秒轮询。

## 交付

本次仅本地修复与测试，尚未部署。聚宽仍使用单文件helper v18，更新时只替换helper；策略正文和私有配置无需修改。服务端修复需部署后才生效。
