# 2026-09-07：收盘后发单与同日委托ID冲突修复

## 范围与切片

服务器当前运行21a8456：零价成交已入账，但重启后继续当日调仓，新增3条拒单记录的broker_order_id与早上的卖单重复。本轮用户批准修复，不主动下单/撤单、不修改历史资金，不改选股与定价；先本地完成回归，部署另行确认。

1. **发单时段**：服务器墙上时间控制目标提交、自动恢复和逐笔派发。休市时仍接收回报、同步账本；不生成/派发新单。保留午夜过期撤单规则。
2. **订单关联**：同日同ID保存候选集，不再后写覆盖。证券、方向与策略标记共同定位；已入账成交优先使用持久化的原订单关联。不能唯一匹配时明确阻断，不猜测归属。
3. **历史缓存与复审**：避免把两个委托的字段混合，补齐跨重启、重复回报和真实缺价规则回归；保持ZERO_FALLBACK及未知费用语义不变。

每片完成后检查差异和测试，再进入下一片。只使用现有服务器组件，不增加聚宽脚本基础设施，不新增后台轮询或交易模式。

## 实现与复审

### 切片1：发单时段

- `SQLiteStrategyAPI`使用服务器的上海时区当前时间，在新目标入口、回调恢复入口和每笔派发前检查时段。请求中的历史`as_of`不能绕过检查。
- 发单窗口为周一至周五09:30（含）至11:30（不含）、13:00（含）至15:00（不含）。此处没有增加节假日日历，不能宣称完整识别所有非交易日；仅约束StrategyLedger自动执行，不改变原生broker接口。
- 午休/盘后仍查询并入账迟到成交，但不推进未完成目标、不派发待发订单。午夜目标过期及撤单规则不变。
- 48项API测试通过并复审，包括伪造请求时间、收盘前后边界、周末、逐笔检查、待发队列及休市时迟到成交入账。

### 切片2：订单与成交关联

- 本地订单不再由`(交易日, broker_order_id)`单值字典覆盖。关联时先筛交易日、证券、方向（委托还匹配数量），再用精确client_tag区分候选。
- 成交可用柜台合同号`order_sysid`关联已识别的委托；已入账成交则使用数据库持久化的原本地订单关联，不因后来同ID拒单改变归属。
- 旧回报被误附新策略标记、但证券/方向明确属于唯一旧单时，仍能识别旧单。相同证券/方向/ID且无唯一标记或合同号时，明确报关联不唯一，不猜测。
- 保留成交数量、方向、价格、时间等不可变字段验证；即使没有对应券商委托行，显式返回的成交方向也不能被本地方向覆盖。
- 委托终态与活动单检查改用本地订单ID；旧卖单的FILLED不能再结束新的买单。历史资金与已入账成交不重写。

### 切片3：缓存与整链路回归

- SQLite v11只改变`broker_order_history`：同日同ID允许多条带`observation_no`的独立观察记录；已有行逐字段迁移为第0条，不修改旧迁移校验和。
- 历史持久化及当前查询合并均按证券、方向、委托数量、标记、合同号和委托时间区分身份；状态、成交数量和价格不是身份字段。信息不足且存在冲突时保留独立观察，不拼接两个委托。
- QMT等待诊断缓存附加到查询行前检查证券、方向及已有标记，避免把另一委托的备注和等待快照粘到旧回报上。
- 加入同日同ID、同证券同方向、缓存重开后合并与对账、重复回报不重复记账，以及已入账成交数量/方向/价格真实变化仍报错的复现测试。ZERO_FALLBACK、费用未知、策略契约与导出门禁继续回归。

## 最终验收

- 443项本地测试通过：`tests/server/strategy`、服务器策略配置、QMT订单/成交与等待适配、历史适配、飞书、聚宽运行时、good_etf策略契约和导出门禁。
- 关键语法/未定义名称检查（flake8 E9/F63/F7/F82）及`git diff --check`通过。
- 复审确认所有发单路径均经过最后一层时段检查；休市回调只同步账本；同ID匹配不再覆盖本地订单；缓存迁移保存旧行；无选股、权重、定价或聚宽部署物改动。
- 测试使用本地模拟broker；依赖包的既有弃用警告不影响通过结果，不能据此声称真实QMT链路已验收。

## 部署边界

实现验收阶段只完成本地修改和测试，当时未推送或部署；随后按用户“部署”指令执行，结果见下。整个过程不主动下单/撤单，也不修改交易开关或白名单。

下一次部署仅需更新服务器fork，不需要因本次变更重新上传聚宽策略/helper/config。应在停服后备份`.data`中的账本及配置，再升级到v11；降级不能只回退代码，需使用匹配的数据库备份。部署后只读核对同日冲突委托和原成交关联、对账状态，并确认盘后没有新增订单；不得通过改写旧成交或伪造回报让验收通过。

此前已经混合或丢失的历史缓存字段不能靠v11迁移凭空恢复。新关联规则可修正“现有成交被指向新订单”的计算路径，但剩余历史数据是否足够，仍须部署后用服务器实际快照验证。

## 2026-09-07部署结果

- 代码提交`a465f68`已推送到个人fork的`codex/joinquant-execution-modes`分支；服务器目录`C:\Users\Administrator\dev\bullet-trade_baihua`从`21a8456`快进至该提交，工作区干净。
- 部署前QMT ready，但账本BLOCKED，存在3组同日委托ID重复，关联冲突为`broker fill id was reused with different fields`；本地活动订单为0。
- 确认58620的进程命令指向fork的`.data/.env`后停止任务及服务进程，创建停服备份：`.data/backups/strategy-ledger-before-a465f68-20260907-234055.db`及同目录`env-before-a465f68-20260907-234055.env`。
- 离线迁移至v11，`integrity_check=ok`、外键错误0；迁移后fills、strategy_orders、strategy_accounts与停服备份逐字段一致，服务器当时的发单时段检查为false。
- 重启`BulletTradeBaihua-Server`后，58620由新PID3500监听；QMT仍为原XtMiniQmt PID4228，没有重启。
- 23:42调用`strategy.get_snapshot`只读查询接口及`admin.health`验收：对账READY、blockers为空、strategy_ledger_ready=true、QMT ready、broker connected。没有调用提交目标、下单或撤单接口。
- 重启查询后16笔成交和19条订单仍与备份逐字段一致，活动订单0：未新增盘后订单，未重复记账，也未改写原有成交。23:43抽查本次重启后的日志，未见ERROR或成交ID冲突。
- `.env`与备份哈希一致：交易保持true，白名单仍只有`good_etf_remote`，缺价策略仍为ZERO_FALLBACK；没有更改账号、凭据、能力文件或交易授权。
- 旧上游目录`C:\Users\Administrator\dev\bullet-trade`仍为`cd42a97`，原有`logs/app.log`修改及`command.txt`未跟踪状态保持不变，未进行写操作。

本次部署不需要用户更新聚宽文件。当前验证证明盘后恢复和现有账本关联已正常；没有进行交易时段主动发单测试，后续开盘成交仍需按正常运行日志观察。
