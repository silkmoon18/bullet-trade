# 大 QMT 桥接与上游合并

## 2026-09-12 现状 / 决策

- fork 基线 `8c45cc7`；官方上游 `main=dca40bc`，版本 `0.10.0b3`（Beta）。
- 当前独立 QMT GoodETF 原生/回测版本先提交保存；账户号只保存在忽略的本地备份，公开源码使用空账户配置。无关 `scripts/backtest_quantskills_a_share.py` 不纳入本任务。
- 回到聚宽决策 -> 原 helper -> 服务器 StrategyLedger / 执行器 -> 大 QMT 通用桥接；不复制策略、资金账本、先卖后买或追单到 QMT 脚本。
- JQ 原生撮合、策略过滤/排序/权重及参考价格不变。旧独立版作为备选保留，不与同策略的服务器执行并跑。
- 本任务只做本地代码、文档与离线测试；不推送、不部署、不调用柜台、不修改交易开关。

## 上游评估

直接受益：大 QMT 下单/撤单确认、客户标识、真实成交时间、唯一订单关系认领成交、持仓/行情字段修复；远程写入幂等与未知态；ETF T+0 证券契约。

保留但不启用：华鑫 TORA/归集功能、新行情基础设施。历史行情标准化有利于通用 BT，但不替换 GoodETF 的聚宽选股数据。

上游要求真实 broker 写入携带稳定 idempotency_key；合并时须与 fork 的目标级幂等和策略账本兼容。不能以官方 helper 覆盖 fork 的双账户/API/通知/风控门面。

## 切片及验收

1. S0 保存工作：原生 QMT 测试；脱敏提交；创建隔离开发分支。
2. S1 上游合并：审查冲突，保留双账户、账本、原生 JQ 执行、费用/历史及执行价格；运行相关回归后提交合并。
3. S2 通用桥接：复用 big_qmt 映射，新增可选本机命令/事件通道；QMT 单文件通过定时器非阻塞收发，原生 API 在 QMT 回调线程执行；不在 init 中阻塞 HTTP 循环。
4. S3 执行闭环：委托/成交/错误/行情接回服务器监听，复用历史与幂等；部分成交、未知提交、重连及重复消息测试；不引入第二账本。
5. S4 交付：GBK 单文件、顶部配置、部署/迁移指导和验收清单；回归及自审后提交。客户端模拟账户验收明确列为未完成，不伪造能力结果。

每一切片先审查再继续；若上游已解决某问题直接复用，避免另造同类机制。

## Session

- 已读取项目边界规则，检查工作区，fetch 官方上游和标签。
- 上游最新为 Beta 3；本机现有 main 分叉较多，采用显式 merge 而非覆盖源码或强制重置。
- 待记录：切片结果、测试计数、冲突决策与未完成项。

### S0 / S1 审查

- S0：`8512457` 保存独立 QMT 版本，65 项原生/回测测试通过；账户配置已清空，私有备份未提交。
- S1：12 项冲突逐项处理；JQ helper 与 `strategies/joinquant/` 相对提交前无代码差异。保留 fork StrategyLedger、看板、双账户、费用已知标志，合入上游行情/交易/生命周期修复。
- 合并修正：类型声明补上上游可选参数；GBK 测试兼容 CRLF；远程客户端保留 broker_called=false 的明确拒单证据，未知提交异常不再被 TimeoutError 分支改写成客户端超时。未恢复上游完整接管 helper（与 fork API 不兼容）。
- 1863 项回归通过：服务器/账本/大QMT、JQ运行时/原生执行/选股、类型与导出、原生QMT及合并契约。
- 扩展 core/unit 全量收集遇到三项外部私有模块缺失：test_live_engine.py、test_formal_runtime_retry.py、test_v2_bridge.py 依赖仓外 strategies/bt_strategies；不伪造依赖或宣称全套通过。
- 上游不再使用 broker RPC 的 TTL 淘汰，保留进程内稳定幂等与容量限制；fork 的策略目标持久幂等不变。
- 下一切片只增加可选桥接传输，保留上游 HTTP 大 QMT 后端作为兼容路径。
