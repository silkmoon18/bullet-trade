# 大 QMT 首次接入只需要填写这三个值。
# 复制为 .env.bigqmt 后修改；不要提交真实账号、密码或 token。

QMT_SERVER_TOKEN=please_generate_a_new_client_token
QMT_ACCOUNT_ID=please_enter_qmt_account_id
BIG_QMT_GATEWAY_PASSWORD=please_match_helper_gateway_password

# 默认值已适合“大 QMT 与 BulletTrade server 在同一台 Windows 机器”场景：
# - server type: big_qmt（启动命令中指定）
# - public port: 58620
# - gateway URL: http://127.0.0.1:9000
# - data/broker modules: enabled
# 高级配置见 docs/big-qmt-server.md 与 docs/config.md，不要一次复制整份配置。
